version: "3.9"

services:  
  xupnpd:
    build: .
    image: archlinux
    container_name: xupnp
    network_mode: host
    volumes:
      - /playlists/:/xupnpd/playlists/
      - /config/:/xupnpd/config/
    restart: always
    
